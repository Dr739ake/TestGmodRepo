#- name: FTP-Deploy-Action
#  uses: SamKirkland/FTP-Deploy-Action@3.1.1
#  with:
#    ftp-server: alpha.gg-community.de
#    ftp-username: ${{secrets.FTPUSERNAME}}
#    ftp-password: ${{secrets.password}}
#    local-dir: garrysmod/addons/
#    remtoeDir: "garrysmod/addons/"


name: FTP Test
on:
  push:
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: FTP-Deploy-Action
        uses: SamKirkland/FTP-Deploy-Action@3.1.1
        with:
          ftp-server: alpha.gg-community.de
          ftp-username: ${{secrets.FTPUSERNAME}}
          ftp-password: ${{secrets.FTPPASSWORD}}
          local-dir: garrysmod/addons/
      # - run: echo "ls" | ftp ${{ env.FTP_USER_NAME }}:${{ env.FTP_USER_PASS }}@ftp-server/ftp
      # - name: file*delete.md
      #   uses: ./.github/actions/ftp-delete-action
      #   with:
      #     host: ftp-server
      #     user: ${{ env.FTP_USER_NAME }}
      #     password: ${{ env.FTP_USER_PASS }}
      #     workingDir: data
      #     ignoreSsl: 1
      #     remoteFiles: "file*.!(keep).md"


           